describe("Smartcar API Test Suite", () => {
  //test for vehicle:id
  it("should return the the vin, color, door count, and drive train given a vehicle id", () => {
    let a = true;
    expect(a).toBe(false);
  });

  //test for vehicle:id/doors
  it("should return the location and status of all doors", () => {
    let a = true;
    expect(a).toBe(false);
  });

  //test for vehicle:id/fuel
  it("should return the fuel range of the vehicle", () => {
    let a = true;
    expect(a).toBe(false);
  });

  //test for vehicle:id/battery

  it("should return the battery range of the vehicle", () => {
    let a = true;
    expect(a).toBe(false);
  });

  //test for vehicle:id/engine
  it("should start or stop the vehicles engine", () => {
    let a = true;
    expect(a).toBe(false);
  });
});
